# generated by XcodeGen — regenerate with `xcodegen generate`
Cryoframe.xcodeproj/

# Xcode / SwiftPM build output
build/
DerivedData/
.build/
*.xcuserstate
xcuserdata/

# spike build artifacts (sources are tracked; binaries are not)
spike/fs_snapshot_spike
spike/*.dSYM

# Claude Code local session settings — never publish. The one exception is the
# shared settings.json, which turns off agent attribution for cloud sessions.
.claude/*
!.claude/settings.json

# release artifacts — attach to GitHub Releases, not the repo
dist/

# macOS
.DS_Store
